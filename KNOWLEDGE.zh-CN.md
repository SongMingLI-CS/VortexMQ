# VortexMQ 知识讲义（大一精读本）

> 给零基础。不要求写过后端。读完应能用自己的话讲清每个概念，并指到仓库里对应的代码。
>
> [技术白皮书](WHITEPAPER.zh-CN.md) 讲项目怎么跑。本讲义讲跑起来用到的每一块知识。正文按章拆开，方便一节一节啃。

---

## 怎么读

不要一天读完。建议：

1. 先把下面「第零课」读两遍。后面所有章节都默认你已经有这些画面。
2. 再按章打开 `knowledge/` 里的文件。每一条都按同一套节奏写：
   - 从你会的东西讲起
   - 用带数字的时间线走一遍
   - 如果没有它会怎样
   - 常见误解
   - 在 VortexMQ 里精确对应到哪一段
   - 停下来想一想
3. 标了「深入」的条目，是调度内核。读不懂就先把时间线在纸上画出来，再回头看代码。
4. 章末的思考题没有标准答案文件，对着源码自测即可。

---

## 目录

| 章 | 文件 | 内容 | 建议用时 |
|----|------|------|----------|
| 第零课 | 本文下方 | 进程、端口、HTTP、JSON、表、异步 | 60–90 分钟 |
| 一 | [knowledge/ch01-python.md](knowledge/ch01-python.md) | 类型、协程、信号、标准库 | 2 小时 |
| 二 | [knowledge/ch02-web.md](knowledge/ch02-web.md) | HTTP、FastAPI、校验、状态码 | 2 小时 |
| 三 | [knowledge/ch03-postgres.md](knowledge/ch03-postgres.md) | 表、事务、行锁、CAS、死锁 | 3 小时 |
| 四 | [knowledge/ch04-redis.md](knowledge/ch04-redis.md) | Stream、PEL、ZSet、Lua、选主锁 | 3 小时 |
| 五 | [knowledge/ch05-distributed.md](knowledge/ch05-distributed.md) | SSOT、Outbox、幂等、选主、停机 | 3 小时 |
| 六 | [knowledge/ch06-security.md](knowledge/ch06-security.md) | API Key、哈希、租户隔离 | 2 小时 |
| 七 | [knowledge/ch07-algo.md](knowledge/ch07-algo.md) | 图、Kahn、BFS、退避、XCom | 2 小时 |
| 八 | [knowledge/ch08-observability.md](knowledge/ch08-observability.md) | 指标、Grafana、探活、日志 | 1 小时 |
| 九 | [knowledge/ch09-engineering.md](knowledge/ch09-engineering.md) | Docker、启动顺序、压测 | 1 小时 |

67 条编号跨文件连续：第 1–8 在第一章，9–15 在第二章，依此类推。附录总表仍在第零课文末。

---

## 第零课：先把计算机里的房间认全

后面会谈到协程、行锁、消费者组。那些词都建立在本课的画面上。本课不出现 VortexMQ 的内部算法，只解决一件事：**系统里的人是谁，他们靠什么说话。**

### 0.1 程序、进程、线程

**程序** 是磁盘上的一堆说明，比如仓库里的 `.py` 文件。它自己不会动，像一份食谱。

**进程** 是「这份说明正在被执行」。你双击、或在终端里敲 `python -m app.worker`，操作系统会：

1. 分配一块只属于它的内存。
2. 给它一个编号，叫 **PID**（process id）。
3. 从说明的第一行开始往下做。

同一份程序可以同时变成多个进程。Compose 里 API 和 Worker 用的是同一份代码、同一个 Docker 镜像，但是两个进程：内存不共享，API 崩溃不会把 Worker 的变量一起抹掉。它们要交换信息，只能走约定好的通道——本项目里就是 PostgreSQL 和 Redis。

**线程** 是进程内部更细的执行线索。一个进程可以有多个线程，它们 **共享** 这块内存。共享带来方便（都看得见同一个变量），也带来麻烦（两个人同时改同一个数会乱）。VortexMQ 的主路径尽量不用「自己开很多线程去抢同一块业务状态」，而是用协程（第一章第 2 条）在一条主线索里切换。bcrypt 那种特别耗 CPU 的活，才会临时请一条线程帮忙（第 6 条）。

**对照生活。**

| 词 | 生活 | 计算机 |
|----|------|--------|
| 程序 | 食谱 | `.py` / 镜像 |
| 进程 | 按食谱正在营业的一家店 | `python -m app.worker` 这一次运行 |
| 线程 | 店里的一个员工 | 进程里的一条执行线索 |
| PID | 营业执照号 | 操作系统给的编号 |

停下来想：为什么 API 和 Worker 要做成两个进程，而不是一个进程里两个函数轮流跑？提示：扩容、崩溃隔离。答案在第五章第 39 条，现在先记住「它们是两家店」。

### 0.2 客户端、服务端、IP、端口

**客户端** 是主动发起对话的一方：你的浏览器、`curl`、压测脚本。

**服务端** 是在某个门口一直等别人来的一方：VortexMQ 的 API。

电脑在网络上的门牌是 **IP 地址**。一台电脑（或一个容器）上可以同时开很多服务，用 **端口** 区分门口：

| 端口 | 谁在听 | 你拿它干什么 |
|------|--------|----------------|
| 8000 | API | 提交任务、查结果 |
| 8001 | Worker 的指标服务 | 给 Prometheus 抓数字 |
| 5432 | PostgreSQL | 账本 |
| 6379 | Redis | 铃铛 |
| 9090 | Prometheus | 存指标 |
| 3000 | Grafana | 画大盘 |

`http://127.0.0.1:8000` 的意思是：本机（127.0.0.1）的 8000 号门口。Docker 把容器内部的端口映射到你笔记本上，所以你能在浏览器里打开它。

**`0.0.0.0` 和 `127.0.0.1` 的差别** 现在就可以建立：进程声明「我听 127.0.0.1」等于只接受自己家里的敲门；声明「我听 0.0.0.0」等于所有网卡上的门都开。容器里的 API 必须听 0.0.0.0，否则你在笔记本浏览器里敲不开。细节见第二章第 10 条。

### 0.3 HTTP：客人递进来的纸条长什么样

HTTP 是一种「请求–响应」约定。客户端发一张纸条，服务端回一张纸条。一次对话通常到此结束（后面可以再开一次新对话）。

一张请求纸条至少有这些部分：

1. **方法。** `POST` 表示「请帮我新建/受理一件事」；`GET` 表示「请把已经有的东西给我看」。不要用 GET 去提交任务，也不要靠 POST 的地址来「只是问问」。
2. **路径。** 例如 `/api/v1/tasks`。同一台服务器用路径区分不同柜台。
3. **头（Header）。** 信封上的附注。本项目最重要的头是 `X-API-Key`：你的钥匙。身份放在头里，不放在 JSON 身体里，原因见第六章第 48 条。
4. **身体（Body）。** 信封里面的内容。提交任务时是一段 JSON。

响应纸条也有：

1. **状态码。** 三位数，见第二章第 13 条。先看码，再看身体。
2. **身体。** 通常也是 JSON，例如刚生成的 `task_id`。

完整的一次提交，用日常语言翻译如下：

```text
客人：POST /api/v1/tasks
信封上写：我的钥匙是 vxk_……，内容类型是 JSON
身体写：任务类型 email.send，收件人是 ops@example.com

API：201
身体写：收下来了，编号是 3fa8…，状态 PENDING
```

大约 3 秒后客人再问：

```text
客人：GET /api/v1/tasks/3fa8…/result
信封上写：同一把钥匙

API：200
身体写：SUCCESS，以及模拟执行的 result_data
```

若还没做完，API 回 **202**，不是错误。202 的意思是「我认这张单，请稍后再问」。

### 0.4 JSON：给机器看的填空表

JSON 是一种用纯文本写结构的格式。本项目的请求身体、数据库里的 `payload` 列、Redis 消息字段，都会碰到它。

```json
{
  "task_type": "email.send",
  "payload": { "to": "ops@example.com" },
  "priority": 0
}
```

你只需要认三种东西：

- **对象**：花括号 `{ }`，里面是「名字: 值」。
- **数组**：方括号 `[ ]`，一列值。工作流的 `nodes`、`depends_on` 是数组。
- **值**：字符串（必须用双引号）、数字、true/false、null、或者再嵌套对象/数组。

常见翻车：JSON 不能有注释；字符串必须双引号不能单引号；最后一个字段后面不能多一个逗号（有的工具能忍，严格解析会拒绝）。Pydantic（第二章第 11 条）会在门口拦住形状不对的 JSON。

### 0.5 表、行、列、主键

PostgreSQL 把数据放在 **表** 里。表就是一张电子表格：

- **列** 规定每一格是什么类型：这格必须是时间，那格必须是一段 JSON。
- **行** 是一条记录。每一个任务在 `task_records` 里占一行。
- **主键** 是这一行的身份证，不能空、不能和别人重复。本项目任务的主键是 `task_id`（UUID，第一章第 5 条）。

读表时可以带条件：「只给我 `tenant_id` 等于这把钥匙对应公司、且 `task_id` 等于客人问的那个编号的行」。条件写得不严，就会读到别人的行。这是第六章第 49 条的根源。

**磁盘 vs 内存。** PostgreSQL 把表落在磁盘上，崩溃重启后行还在。Redis 主要把数据放在内存里，极快，但我们仍然不拿它当「任务是否存在」的证据——内存可以清，命令可以下错。第零课只需记住：**账本在磁盘型的 PostgreSQL 里。**

### 0.6 同步请求为什么不够，异步任务是什么

假设接口内部真的去发邮件，时间线会变成：

```text
0.0 秒  客人按下「发送」
0.1 秒  API 开始连邮件服务器
2.8 秒  邮件服务器慢，API 一直占着这条 HTTP 连接
2.8 秒  客人浏览器转圈，窗口只能同时接待有限个转圈的人
```

若邮件服务器挂了，客人刷新，可能发出两封。

异步任务把时间线拆开：

```text
0.00 秒  客人 POST 任务
0.02 秒  API 在 PostgreSQL 插入一行 PENDING，COMMIT
0.03 秒  API 按 Redis 铃铛
0.04 秒  API 回 201 和 task_id    ← 客人的 HTTP 到此结束
0.05 秒  Worker 被铃铛叫醒
0.06 秒  Worker 把行改成 RUNNING
3.06 秒  模拟执行结束，行改成 SUCCESS，再摘铃铛
3.20 秒  客人 GET result，拿到 200
```

客人不等炒菜。炒砸了（`force_fail`）也有重试和死信，那是第五章的事。第零课只要建立：**接口的职责是收单并给编号，不是把事情做完。**

### 0.7 VortexMQ 这座房子里的人

把 0.1–0.6 装进本项目：

```text
客人 curl / 压测
    │  HTTP + API Key + JSON
    ▼
API 进程（控制面）
    │  ① 写 PostgreSQL
    │  ② 按 Redis 铃（Stream 或 ZSet）
    │  后台：Leader 才跑 Sweeper（补漏按的铃）
    │        和 Dispatcher（把到期的定时器推进铃铛）
    ▼
PostgreSQL = 账本
Redis      = 铃铛 + 定时器
    │
    ▼
Worker 进程（数据面）
    取铃 → 抢账本行 → 假装干活 3 秒 → 改账本 → 摘铃
```

本课之后你读任何一条「深入」，先问三个问题：

1. 这一步改的是账本，还是铃铛？
2. 如果进程在这一步之后立刻被杀，账本和铃铛各处于什么状态？
3. 谁负责把它们重新对齐？

答得上来，内核就开始通了。答不上来，回到这一节画时间线。

### 0.8 你还不需要会的

本讲义 **不会** 先教你把 Python 从零写到能独立开发。读代码时把 `async def` 当成「这是一个可能要等待的函数」，把 `await xxx()` 当成「这里可能去问数据库或 Redis，问的时候可以让出时间片」。语法课不是目标；目标是概念能落到文件名。

你也不需要先修完操作系统、数据库、计算机网络三门课。讲义会在用到的地方把课内定义用大白话给一遍。若某条和课堂上的严格定义打架，以课堂为准，把本讲义当成「这个项目里它具体指什么」。

---

## 附录：67 条总表

| 编号 | 章 | 知识点 | 深度 |
|------|----|--------|------|
| 1–8 | [一](knowledge/ch01-python.md) | Python 与运行时 | 入门–深入 |
| 9–15 | [二](knowledge/ch02-web.md) | Web API | 入门–熟练 |
| 16–25 | [三](knowledge/ch03-postgres.md) | PostgreSQL 与 ORM | 入门–深入 |
| 26–35 | [四](knowledge/ch04-redis.md) | Redis 与消息 | 入门–深入 |
| 36–45 | [五](knowledge/ch05-distributed.md) | 分布式系统 | 熟练–深入 |
| 46–52 | [六](knowledge/ch06-security.md) | 安全与多租户 | 熟练–深入 |
| 53–57 | [七](knowledge/ch07-algo.md) | 算法与工作流 | 入门–熟练 |
| 58–62 | [八](knowledge/ch08-observability.md) | 可观测性 | 入门–深入 |
| 63–67 | [九](knowledge/ch09-engineering.md) | 工程交付 | 入门–熟练 |

下一页：[第一章 Python 与运行时](knowledge/ch01-python.md)
